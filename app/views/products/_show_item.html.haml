.col-md-3.col-sm-4.col-xs-6
  .thumbnail.shadow
    .name.text-center
      =link_to product.name, product_path(product.id)
    .image.text-center
      =link_to image_tag(product.images.first.image.url(:list)), product_path(product.id) if product.images.first
    .price.text-center
      = "Цена #{product.price} руб."
      = form_for cart_item do |f|
        = f.hidden_field :product_id
        = f.hidden_field :quantity
        .btn-group
          %button.btn.btn-primary{type:'button', href:product_path(product.id)}
            Подробнее
          %button.btn.btn-success{type:'submit'}
            %span.glyphicon.glyphicon-shopping-cart
            Купить

-#<div class="price" style="margin-top:-7px;margin-bottom:-5px;">
-#<span class="price-old"><?php echo $product['price']; ?></span><br/><span class="price-new"><?php echo $product['special']; ?></span>
